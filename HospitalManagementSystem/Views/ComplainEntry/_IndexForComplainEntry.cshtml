@model HospitalManagementSystem.Models.EmergecyMasterModel
<div class="general-box clearfix">
        <div class="general-box clearfix">

       <div class="boxWrapper" id="EmergencyComplainEntryId">
    <div class="boxHeadingPanel" style="text-align:center"><h1>Complain and Examination</h1></div>

<table class="table table-hover tablesorter">
    <thead>

    

    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CheifComplainAndHistory)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClinicalExamination)
        </th>
        <th></th>
         
    </tr>

   @foreach (var item in Model.EmergencyMasterModelList)
   {

    <tr>

             @Html.HiddenFor(modelItem => item.EmergencyMasterId)
         <td>
            @Html.DisplayFor(modelItem => item.CheifComplainAndHistory)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ClinicalExamination)
        </td>
     <td>
 @if (item.CheifComplainAndHistory == null)
 {
         <a href='~/ComplainEntry/EditForComplainEntry?id=@Model.EmergencyMasterId' title="" class="crtComplainEntrylink"><span>Create new</span></a> 
 }
 else
 {
     <a href='~/ComplainEntry/EditForComplainEntry?id=@Model.EmergencyMasterId' title="" class="crtComplainEntrylink"><span>Edit</span></a> 
     
 }     

     </td>
            
    </tr>
   }
  
</table>

@*    @if (Model.EmergencyMasterModelList.Count < 1)
    {


    <p><a href='~/ComplainEntry/EditForComplainEntry?id=@Model.EmergencyMasterId' title="" class="crtComplainEntrylink"><span>Create new</span></a> 

</p>
    }*@


</div></div></div>
<div id="cartooningzing"></div>
<script>
    $(".crtComplainEntrylink").click(function (e) {
        e.preventDefault();
        var data = $('#EmergencyMasterId').val();
        var url = this.href;

        $.get(url, { id: data }, function (res) {

            $("#cartooningzing").html(res);

        });
    });
</script>

