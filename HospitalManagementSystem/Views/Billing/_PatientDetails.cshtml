@model HospitalManagementSystem.Models.BillingModel

<div class="general-box clearfix">

    <div class="general-box clearfix">

        <div class="boxWrapper">
            <div class="boxHeadingPanel">
                <h1>Search Result</h1>
            </div>

            <table class="table table-hover tablesorter">
                <thead>
                    <tr>

                        <th>
                            @Html.DisplayNameFor(model => model.ObjCentralizeBillingViewModel.PatientId)
                        </th>
                        <th>
                            Patient FullName
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.ObjCentralizeBillingViewModel.Age)
                        </th>
                        <th>
                            Address
                        </th>


                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.CentralizeBillingViewModelList.Count() == 0)
                    {
                        <tr>
                            <td colspan="10" style="text-align: center;">Data not found ! </td>
                        </tr>
                    }
                    @foreach (var item in Model.CentralizeBillingViewModelList)
                    {
    
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.PatientId)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.LedgerName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Age)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item. Narration1)
                            </td>
                            <td>@Html.ActionLink("Details", "ShowPatientTranRpts", new { @id=item.PatientId},null)</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
